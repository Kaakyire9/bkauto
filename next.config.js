/** @type {import('next').NextConfig} */
// Next.js 14 uses the App Router by default; remove experimental.appDir to avoid warnings.
const nextConfig = {
  // Add any runtime Next.js configuration here when needed
}

module.exports = nextConfig
